import React, { useContext } from "react";

interface TitleXProps
{
    titleName: String;
}
function TitleX({
    titleName,
}: TitleXProps): React.ReactElement<TitleXProps>
{

    return (
        <>

        </>
    );
}

export default TitleX;
